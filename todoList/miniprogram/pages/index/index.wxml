<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <open-data class="userinfo-avatar" type="userAvatarUrl"></open-data>
    <view>{{openid ? 'openid:' + openid : '未登录'}}</view>
  </view>
  <button wx:if="{{ !openid }}" class="btn-red" bindtap="onGetOpenid">登录</button>
</view>

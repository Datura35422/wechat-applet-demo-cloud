<view class="container">
  <view 
    wx:for="{{typeItem}}" 
    wx:key="{{item.index}}" 
    class="type-item {{item.type}}"
    data-path="{{item.path}}"
    bindtap="toPath"
  >{{item.name}}</view>
</view>

<view class="note-card" catchtap="onDetail" catchlongpress="onRemove">
  <view wx:if="{{ !!note.title }}" class="note-card__title">
    {{note.title}}
  </view>
  <view class="note-card__content">
    {{note.content || '新建笔记'}}
  </view>
</view>
